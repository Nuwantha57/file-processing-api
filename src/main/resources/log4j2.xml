<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Appenders>
        <RollingFile
            name="file"
            fileName="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}file-processing-api.json"
            filePattern="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}file-processing-api-%i.json">

            <JsonTemplateLayout/>

            <SizeBasedTriggeringPolicy size="10 MB"/>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Reduce noisy HTTP logs -->
        <AsyncLogger name="org.mule.service.http" level="WARN"/>
        <AsyncLogger name="org.mule.extension.http" level="WARN"/>

        <!-- FIX: Suppress "Connection reset" SocketException noise.
             These fire at Netty's raw TCP layer when CloudHub's load
             balancer drops idle keep-alive connections. They are harmless
             and cannot be caught inside a Mule flow error handler. -->
        <AsyncLogger name="io.netty" level="ERROR"/>
        <AsyncLogger name="org.mule.service.http.impl.functional.server.ForwardingToListenerHandler" level="ERROR"/>

        <AsyncRoot level="INFO">
            <AppenderRef ref="file"/>
        </AsyncRoot>
    </Loggers>

</Configuration>